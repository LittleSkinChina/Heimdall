{
  "id": "${pluginId}",
  "name": "${name}",
  "description": "${description}",
  "version": "${version}",
  "authors": [
    "${author}"
  ],
  "url": "${url}",
  "dependencies": [
    {
      "id": "luckperms",
      "optional": true
    },
    {
      "id": "floodgate",
      "optional": true
    }
  ],
  "main": "${group}.velocity.VelocityMain"
}